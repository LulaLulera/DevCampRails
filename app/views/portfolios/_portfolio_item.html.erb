<!--<p><%#= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %><p>

<p><%#= portfolio_item.subtitle%><p>-->

<%#= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_ima ge.blank?%>


<div class="col-md-4">
  <div class="card portfolio-box" data-id="<%= portfolio_item.id %>">
    <a href='<%= portfolio_show_path(portfolio_item) %>'>
      <%#= image_tag portfolio_img(portfolio_item.thumb_image, 'thumb'), width: '100%' %>
      <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.blank?%>
    </a>
    <p class="card-text">
      <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span> <%= portfolio_item.subtitle %>
    </p>
  </div>
</div>